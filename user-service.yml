spring:
  datasource:
    url: ${USER_DB_URL:jdbc:postgresql://postgres:5432/user_db}
    username: ${USER_DB_USERNAME:user_app}
    password: ${USER_DB_PASSWORD:user_app}
  jpa:
    hibernate:
      ddl-auto: validate
